class Greeter {
  init(name) {
    this.name = name;
  }

  greet() {
    print "Hello, " + this.name + "!";
  }
}

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

var g = Greeter("world");
g.greet();

for (var i = 0; i < 10; i = i + 1) {
  print fib(i);
}
